# API Adjust - Environment Variables
# Required variables for the application to work

# Storage Configuration
# Workspace inside the API container. Files are fetched via NGINX and saved here.
STORAGE_ROOT=/app/public
DATA_DIR=./data

# Output Configuration
# Leave OUTPUT_BASE_REL_PATH empty to mirror input path with "origin" -> "process"
# OUTPUT_BASE_REL_PATH=uploads/process
OUTPUT_TARGET_SEGMENT=process
# OUTPUT_PATH_PREFIX=upload.process
# INPUT_PATH_PREFIX=uploads

# Web Server Configuration (for file access)
NGINX_INPUT_BASE_URL=http://192.168.210.100:8001/files

# API Configuration
API_PORT=8001
API_HOST=0.0.0.0

# Docker Configuration
API_ADJUST_IMAGE=api-adjust:latest
COMPOSE_PROJECT=api-adjust

# Logging
LOG_LEVEL=INFO
LOG_FORMAT=json

# Environment
ENVIRONMENT=production
DEBUG=false
